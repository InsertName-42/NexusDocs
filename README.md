# NexusDocs
Uses google docs api to generate webpages directly from the familiar and easily modifiable google docs framework that already tracks version history and can be edited from anywhere. Users input google document links, a template, and any number of tags and the site’s logic generates a personalized website accessible to anyone.
